feat: scan all 5221 NSE+BSE stocks across all engines

Updated createUltraQuantUniverse() to use the full Supabase universe
(5221 stocks) instead of the hardcoded 434-stock curated list.

- Curated NSE_STOCK_UNIVERSE is now used as an enrichment overlay:
  if a symbol exists in both, curated sector/industry/marketCap/volume
  takes priority; otherwise Supabase data is used with safe defaults.
- runPredictionScan() also switched back to getUniverseAsync() (5221).
- All scan engines (AI Intelligence, Multibagger, UltraQuant, Live
  Predictions) now show universe = 5221.

Verified: Multibagger scannedUniverse=5221, UltraQuant totalLoaded=5221
