feat: 5000+ stock universe with lazy loading — zero timeout

Architecture:
- getUniverseSync(): returns embedded 434 stocks instantly on cold start
- Background Supabase load upgrades to 5221 stocks after ~3-5s
- Scan caches invalidated on upgrade → next request uses full universe
- ultraQuantCache + multibaggerCache moved to module level (shared across requests)

Performance:
- Pass 1 pre-sort: O(1) hash-based scoring per stock (no candle simulation)
  handles 5000+ stocks in <5ms vs previous 1260-day candle loop
- Full analyzeUltraQuantProfile only on top 100 candidates
- Cold scan: ~200ms (434 stocks)
- Warm scan after Supabase load: ~120ms (5221 stocks)
- Multibagger warm: ~46ms

Tested locally:
- All 4 scan tests pass (cold + warm, UltraQuant + Multibagger)
- No timeouts, all under 200ms response time
- Universe shows 5221 after background load
