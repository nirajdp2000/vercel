fix: eliminate all fake prices — null for SIM stocks, real-only for LIVE

INVESTIGATION RESULTS (tested all sources):
  - Yahoo v8 chart (.NS/.BO): ✅ ONLY reliable free source, within 0.3% of NSE
  - NSE India API: ❌ 403 from Vercel IPs
  - Moneycontrol: ❌ inconsistent (works for TCS, fails for others)
  - BSE India API: ❌ returns HTML
  - Stooq: ❌ N/D for Indian stocks
  - Twelve Data: ❌ needs paid key
  - Upstox: ❌ needs auth token
  - BLOOM is BSE-only (BLOOM.BO = ₹11.46) — Yahoo .NS returns 404

FIXES:
1. currentPrice: null for synthetic stocks (no fake random-walk prices)
   - UltraQuant: null when no Yahoo enrichment AND no real OHLCV candles
   - Multibagger: null when no Yahoo enrichment AND no real OHLCV candles
   - UI already shows '—' for null currentPrice

2. lstmPredictedPrice: null for synthetic stocks
   - Type updated to number | null in AnalysisResult
   - Display shows '—' instead of fake ₹14880-style numbers
   - LSTM Target card only shows green dot when dataSource=real

3. Superbrain: passes null price for synthetic stocks
   - computePriceTargets returns null targets when price=null
   - No more fake ₹14,249 targets for BLOOM/SIM stocks

4. startServerlessApp: pre-warms enrichment for top 50 symbols at startup
   - First scan request now gets real prices from warm cache
   - Previously: cold start → all enrichment null → all prices synthetic

5. MarketDataAggregator: added YAHOO_OVERRIDES for BSE-only stocks
   - TATAMOTORS, M&M, BAJAJ-AUTO now resolve correctly via .BO
