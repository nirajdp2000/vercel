fix: normalize candle order to newest-first so App.tsx reverse works correctly

The intraday endpoint returns oldest-first while historical returns newest-first.
After merging them, the array was inconsistently ordered causing the chart to
show wrong/reversed data.

Fix: sort allCandles by timestamp descending (newest-first) before returning,
matching Upstox historical convention. App.tsx .reverse() then correctly
produces oldest-first for chart rendering.

Verified:
- 5min today only: newest-first, last candle = current time IST
- 5min 28 days: newest-first, last candle = current time IST
- day interval: newest-first, unchanged behavior
