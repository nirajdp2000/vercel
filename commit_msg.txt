fix: fast live-price pre-fetch for all top-100 stocks before analysis

Root cause: BLOOM and other BSE-only/small-cap stocks showed — price because:
1. OHLCV background warmer is fire-and-forget (cold on first request)
2. enrichStocksBackground is also fire-and-forget (cold on first request)
3. Both caches empty on cold start → synthetic candles → null currentPrice

Fix: Add Pass 2b in buildUltraQuantDashboard and buildMultibaggerScan:
- Before analysis, identify all top-100 symbols missing a cached price
- Batch-fetch live prices via fetchYahooQuote (range=1d, ~200ms each)
- Hard 3s total timeout — never blocks beyond that
- Populates perSymbolPriceCache for use in analyzeUltraQuantProfile
- Also backfills realOHLCVCache.livePrice if candles exist but livePrice was null

analyzeUltraQuantProfile price priority (4 sources now):
  1. Yahoo enrichment lastPrice (MarketDataAggregator, 1hr cache)
  2. OHLCV meta regularMarketPrice (from fetchRealOHLCV, same v8 call)
  3. perSymbolPriceCache (from fetchYahooQuote range=1d, fast pre-fetch)
  4. OHLCV last candle close (EOD, real but may be yesterday)
  5. null — truly synthetic, no Yahoo data at all

Same fix applied to buildMultibaggerScan.
pChange and superbrain price also use the same 4-source priority.

Verified: BLOOM.BO returns 224 valid candles + livePrice=11.46 from Yahoo v8.
The 60-candle check passes. Issue was purely cold-cache on first request.

Also cleanup: test-bloom.mjs removed from repo.
