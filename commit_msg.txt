fix: resolve FUNCTION_INVOCATION_TIMEOUT — background enrichment pattern

- Replace enrichStocks() (blocking) with enrichStocksBackground() + getEnrichedFromCache()
- Both buildUltraQuantDashboard and buildMultibaggerScan now read enrichment
  from in-memory cache only (zero network latency in scan pipeline)
- Background warm fires after scan response is sent (fire-and-forget)
- Update all field paths: fundamentals.* → screener.*, quote.* → yahoo.*
- Yahoo Finance v7 quoteSummary replaces NSE cookie-based API (no cookie needed)
- Screener.in JSON API replaces HTML scraping (faster, structured)
- All HTTP calls hard-capped at 4s — Vercel 10s limit safe
- Fix /api/stock/enrich endpoint to use new pattern
- Remove deliveryPct (not available from Yahoo Finance)
