feat: Institutional tab self-contained — auto-loads default stock, no Analytics tab required

- Added DEFAULT_STOCKS list (Reliance, HDFC Bank, Infosys, TCS, ICICI Bank)
- Added selfStock/selfCandles/selfLoading state inside InstitutionalAnalytics
- Auto-fetches 30-day daily candles for selected stock on mount when prop candles absent
- Stock selector dropdown shown in header when no stock selected on Analytics tab
- effectiveCandles/effectiveSymbol/effectiveKey derived: prop values take priority, self-fetched as fallback
- All internal logic (useEffect, derived, heatmap, volume profile) uses effectiveCandles
- Loading indicator shown while self-fetching
- canAnalyze now works with effectiveCandles so AI Deep Scan enabled after self-fetch
