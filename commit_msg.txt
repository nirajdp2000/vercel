perf: prioritize real-data symbols in scan pre-sort

- Pass 1 pre-sort now gives +30 bonus to symbols with real OHLCV in cache
- Ensures cached large-caps (RELIANCE, TCS, etc.) always rank in top 100
- Prevents random small-caps from displacing real-data stocks
- Applied to both UltraQuant and Multibagger scan pipelines
