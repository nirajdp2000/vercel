fix: Superbrain price + ROE/ROCE/PE in DB for all symbols

- loadFundamentalsFromSupabase now accepts setPriceCache callback
  → populates perSymbolPriceCache from last_price in DB
  → Superbrain gets real price for all symbols, not just top 50
- EOD refresh now fetches Screener (ROE/ROCE/PE/Promoter%) for top 50
  → responds immediately, Screener batch runs fire-and-forget
  → fundamentals_cache gets complete data on each daily refresh
- Both scan pipelines pass setPriceCache to loadFundamentalsFromSupabase
